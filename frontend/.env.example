# Backend URL for health check + API routing. On Railway, use the reference
# variable ${{backend.RAILWAY_PUBLIC_DOMAIN}} prefixed with https://.
VITE_BACKEND_URL=

# Supabase (anon key only — never expose service_role client-side)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
